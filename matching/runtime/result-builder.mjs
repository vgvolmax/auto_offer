export function offerRef(c){return {catalog_record_id:c.catalog_record_id,catalog_id:c.catalog_id,source_sha256:c.source_sha256,source_item_id:c.source_item_id}}
export function resolution(candidates,excluded){if(!candidates.length)return excluded.length?'excluded_by_policy':'no_match';const exact=candidates.filter(c=>c.match_level==='exact').length;if(exact)return exact===1?'single_exact':'multiple_exact';return candidates[0].match_level==='equivalent'?'equivalent_only':'alternative_only'}
export function publicCandidate(c,technical,availability='eligible',code) { const effect=technical.level; return {offer_ref:offerRef(c),match_level:effect,availability,checks:[{scope:'class',target:{class_id:c.class_id},outcome:'pass',effect,code:code??technical.reason??'CLASS_MATCH'}],differences: effect==='exact'?[]:[{scope:'attribute',target:{field:'pilot_rule'},outcome:'difference',effect,code:effect==='equivalent'?'EQUIVALENT_RULE_APPLIED':'ALTERNATIVE_RULE_APPLIED'}]}; }
