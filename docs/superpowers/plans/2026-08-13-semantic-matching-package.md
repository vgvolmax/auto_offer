# Semantic matching package implementation plan

1. Define strict semantic catalog and result schemas without modifying legacy contracts.
2. Implement browser-safe package projection, policy filtering, canonical fingerprinting, and cross-file validation.
3. Add a Node Ajv loader and validation-only CLI with structured diagnostics.
4. Write the self-contained external-chat prompt and workflow documentation.
5. Cover package slicing, fingerprints, schemas, invariants, prompt markers, and CLI success/failure; run all legacy and application regressions.

