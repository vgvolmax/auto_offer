# Catalog bundles

This directory is for finished annotated catalog bundles. One file corresponds to one source price list; use `catalog.<catalog_id>.json` as the recommended name. Before committing it, run `npm run validate:catalog-bundle -- <file>`. The source Excel workbook may be stored separately. No real catalog data is included by this change.
