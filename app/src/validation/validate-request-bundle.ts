import type {ValidationIssue,ValidationResult} from '../domain/validation';import {createBrowserValidationContext} from './create-browser-validation-context';
export function validateRequestBundle(bundle:unknown):ValidationResult{const c=createBrowserValidationContext(),v=c.requestBundleValidator,e:ValidationIssue[]=[];if(!v(bundle))e.push(...(v.errors??[]).map(x=>({code:'BUNDLE_SCHEMA_INVALID',path:x.instancePath||'/',message:x.message??'Некорректная структура',details:x})));const b=bundle as any;if(b?.taxonomy_version!==c.taxonomy.taxonomy_version)e.push({code:'TAXONOMY_VERSION_MISMATCH',path:'/taxonomy_version',message:'Версия taxonomy заявки несовместима с приложением'});const lines=b?.request_document?.lines;if(Array.isArray(lines)&&typeof b?.source?.line_count==='number'&&b.source.line_count!==lines.length)e.push({code:'LINE_COUNT_MISMATCH',path:'/source/line_count',message:'Заявленное число строк не совпадает с фактическим'});return{valid:e.length===0,kind:'request_bundle',errors:e,summary:{records:Array.isArray(lines)?lines.length:0,taxonomy_version:b?.taxonomy_version}}}
