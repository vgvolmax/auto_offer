import {useEffect,useRef} from 'react';export function SidePanel({open,onClose,title,children}:{open:boolean;onClose:()=>void;title:string;children:React.ReactNode}){const close=useRef<HTMLButtonElement>(null),previous=useRef<HTMLElement|null>(null);useEffect(()=>{if(open){previous.current=document.activeElement as HTMLElement;close.current?.focus()}else previous.current?.focus()},[open]);if(!open)return null;return <div className="panel-backdrop" onMouseDown={e=>{if(e.target===e.currentTarget)onClose()}}><aside className="panel" role="dialog" aria-modal="true" aria-label={title}><header><h2>{title}</h2><button ref={close} aria-label="Закрыть" onClick={onClose}>×</button></header>{children}</aside></div>}
