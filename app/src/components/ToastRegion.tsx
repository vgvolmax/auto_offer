export interface Toast {id:string;kind:'success'|'error'|'warning';message:string;action?:()=>void;actionLabel?:string} export function ToastRegion({toasts,onClose}:{toasts:Toast[];onClose:(id:string)=>void}){return <div className="toasts" aria-live="polite" aria-atomic="false">{toasts.slice(-3).map(t=><div className={`toast toast--${t.kind}`} key={t.id}><span>{t.message}</span>{t.action&&<button onClick={t.action}>{t.actionLabel}</button>}<button aria-label="Закрыть уведомление" onClick={()=>onClose(t.id)}>×</button></div>)}</div>}
