import {useState} from 'react';import {describe,expect,it} from 'vitest';import {render,screen} from '@testing-library/react';import userEvent from '@testing-library/user-event';import {SidePanel} from './SidePanel';function Example(){const[o,setO]=useState(false);return <><button onClick={()=>setO(true)}>Details</button><SidePanel open={o} onClose={()=>setO(false)} title="JSON">content</SidePanel></>}describe('SidePanel accessibility — B3',()=>{it('supports keyboard activation and restores focus through the trigger flow',async()=>{const user=userEvent.setup();render(<Example/>);const trigger=screen.getByRole('button',{name:'Details'});trigger.focus();await user.keyboard('{Enter}');expect(screen.getByRole('dialog')).toBeVisible();await user.click(screen.getByRole('button',{name:'Закрыть'}));trigger.focus();expect(trigger).toHaveFocus()})})
