export interface CatalogBundle { taxonomy_version:string; catalog:{catalog_id:string;source_file_name:string;source_sha256:string;item_count:number}; items:Array<{catalog_item?:{annotation?:{status?:string}}}>; [key:string]:unknown }
export interface CatalogRecord { recordId:string;catalogId:string;sourceFileName:string;sourceSha256:string;taxonomyVersion:string;addedAt:string;updatedAt:string;enabled:boolean;itemCount:number;validatedCount:number;needsReviewCount:number;size:number;bundle:CatalogBundle }
export function createCatalogRecord(bundle:CatalogBundle, size=0, now=new Date().toISOString()):CatalogRecord { const statuses=bundle.items.map(x=>x.catalog_item?.annotation?.status); return {recordId:crypto.randomUUID(),catalogId:bundle.catalog.catalog_id,sourceFileName:bundle.catalog.source_file_name,sourceSha256:bundle.catalog.source_sha256,taxonomyVersion:bundle.taxonomy_version,addedAt:now,updatedAt:now,enabled:true,itemCount:bundle.items.length,validatedCount:statuses.filter(x=>x==='validated').length,needsReviewCount:statuses.filter(x=>x==='needs_review').length,size,bundle}; }
