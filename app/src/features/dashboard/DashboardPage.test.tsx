import {beforeEach,describe,expect,it} from 'vitest';import {render,screen,waitFor} from '@testing-library/react';import {MemoryRouter} from 'react-router-dom';import {DashboardPage} from './DashboardPage';import {resetDatabaseConnection} from '../../storage/database';
beforeEach(async()=>{resetDatabaseConnection();await new Promise<void>(ok=>{const r=indexedDB.deleteDatabase('auto-offer');r.onsuccess=()=>ok()})});describe('dashboard — A1, H1',()=>{it('guides first launch toward the first catalog',async()=>{render(<MemoryRouter><DashboardPage/></MemoryRouter>);expect(await screen.findByText('Каталоги ещё не подключены')).toBeVisible();expect(screen.getByRole('link',{name:'Добавить первый каталог'})).toBeVisible()});it('shows textual restoration feedback',async()=>{render(<MemoryRouter><DashboardPage/></MemoryRouter>);expect(screen.getByRole('status')).toHaveTextContent('Восстанавливаем');await screen.findByText('Каталоги ещё не подключены')})})
