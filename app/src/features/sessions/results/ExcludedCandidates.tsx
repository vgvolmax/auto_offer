import type { ExcludedCandidateReviewView } from "../../../domain/matching/match-result-review";import { getReasonCodeLabel } from "../../../domain/matching/match-result-labels";import { CandidateChecks } from "./CandidateChecks";
export function ExcludedCandidates({items}:{items:ExcludedCandidateReviewView[]}){if(!items.length)return null;return <details><summary>Исключённые предложения: {items.length}</summary>{items.map(x=><article key={x.key}><h4>{x.productLabel}</h4><code>{JSON.stringify(x.offerRef)}</code><p>{x.matchLevel} · {x.availability}</p><ul>{x.exclusionCodes.map(c=><li key={c} title={c}>{getReasonCodeLabel(c)}</li>)}</ul><CandidateChecks checks={x.checks} differences={x.differences}/></article>)}</details>}
