import {afterEach,describe,expect,it,vi} from 'vitest';import {downloadJsonFile,downloadTextFile} from './download-file';
describe('downloads',()=>{Object.defineProperty(URL,'createObjectURL',{value:()=>'',configurable:true,writable:true});Object.defineProperty(URL,'revokeObjectURL',{value:()=>{},configurable:true,writable:true});afterEach(()=>vi.restoreAllMocks());it('sets filename/MIME and revokes object URL',async()=>{const create=vi.spyOn(URL,'createObjectURL').mockReturnValue('blob:test'),revoke=vi.spyOn(URL,'revokeObjectURL').mockImplementation(()=>{}),click=vi.spyOn(HTMLAnchorElement.prototype,'click').mockImplementation(()=>{});downloadJsonFile('value.json',{b:2});expect(create).toHaveBeenCalled();const blob=create.mock.calls[0][0] as Blob;expect(blob.type).toBe('application/json;charset=utf-8');expect(blob.size).toBe(new TextEncoder().encode('{\n  "b": 2\n}\n').length);expect(click).toHaveBeenCalled();expect(revoke).toHaveBeenCalledWith('blob:test')});it('downloads text with requested MIME',()=>{vi.spyOn(URL,'createObjectURL').mockReturnValue('blob:text');vi.spyOn(URL,'revokeObjectURL').mockImplementation(()=>{});vi.spyOn(HTMLAnchorElement.prototype,'click').mockImplementation(()=>{});downloadTextFile('prompt.md','text','text/markdown');expect(URL.createObjectURL).toHaveBeenCalled()})});
